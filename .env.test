# 测试环境构建默认值。机器专用覆盖写入 .env.test.local（已被忽略）。

VITE_APP_TITLE=Runlume 标准后台设计（测试）
VITE_APP_STORAGE_PREFIX=runlume_test.
VITE_APP_API_BASEURL=/
VITE_APP_PLATFORM_WEB_BASEURL=https://runlume.app
VITE_APP_DISABLE_DEVTOOL=false

# 测试环境带 sourcemap，方便对着堆栈排查
VITE_BUILD_SOURCEMAP=true
VITE_BUILD_COMPRESS=
